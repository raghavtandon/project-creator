# =============================================================================
# CLIENT CONFIDENTIALITY PROTECTION
# =============================================================================
# All client work happens in companions/ - this folder is NEVER committed.
# Tracking contains references to active client companions - also NEVER committed.
# This ensures client names, companion details, and work products stay private.
# =============================================================================

# Companion projects - each has its own independent git repo
# This is the PRIMARY confidentiality protection
companions/

# Legacy projects directory (migrated to companions/ by /configure)
projects/

# Project tracking - contains client/companion references and activity logs
tracking/

# Project ideas - may contain proprietary or confidential concepts
project-ideas/

# =============================================================================
# COMPANION KITS
# =============================================================================

# Private companion kits - org-specific personas, capabilities, and library
# Each org directory can be independently version-controlled with its own git repo
companion-kits/private-kits/

# Reference projects in public personas - these reference private paths
# that won't exist for other users. Each user builds their own over time.
companion-kits/public-kits/personas/*/reference-projects.md

# =============================================================================
# LEGACY PROJECT TYPES (kept during migration, remove after confirmed)
# =============================================================================

# Private project types - proprietary type accelerators not for public release
project-types/private/

# Reference projects in public types - these reference private project paths
project-types/public/*/reference-projects.md

# =============================================================================
# WORKBENCH PREFERENCES
# =============================================================================

# Companion Workbench stores user preferences here (D56).
# Personal settings — not shared across machines or users.
.workbench/

# Shared local data (not for version control)
.shared/

# =============================================================================
# USER-SPECIFIC / LOCAL FILES
# =============================================================================

# Local Claude settings (may contain user-specific permissions)
.claude/settings.local.json

# Claude Code worktrees (local agent workspace artifacts)
.claude/worktrees/

# Any file with .local. in the name (convention for local overrides)
*.local.*

# Environment and secrets (should never be committed)
.env
.env.*
*.secret
*credentials*
*.key
*.pem

# =============================================================================
# GENERATED / BUILD ARTIFACTS
# =============================================================================

# Build progress tracking (generated by /build, contains project state)
**/build-progress.md

# =============================================================================
# OS AND EDITOR FILES
# =============================================================================

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux
*~

# Editor files
*.swp
*.swo
.idea/
.vscode/
*.sublime-*

# =============================================================================
# ARCHIVE (optional - uncomment to exclude archived docs)
# =============================================================================
# docs/archive/
